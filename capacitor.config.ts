import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sistema.de.atendimento',
  appName: 'sistema-de-atendimento',
  webDir: 'www'
};

export default config;
